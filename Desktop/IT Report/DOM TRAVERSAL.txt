DOM TRAVERSAL
How to alter the contents of elements:
React framework

NPM is a code someone has written which you can use in your project When you write a code and make it a package which other people can install in their code, it becomes and NPM

Vite is made for frontend apps.

Bringing a UI design to life using react. It was made for building complex and scalable applications.
React is considered  library not a framework because its quite flexible.
eslint ensures that your code is clean.

npm install will install all dependencies and devDependencies.

Don't tamper with the content of node_modules, if anything happens to it, delete the folder and run npm install again.

Here we are dealing with local dev so this is http not https
react uses jsx, it 
jsx extension: why? the .jsx is what react uses, java scrip xternal, its a way of combining both js and html together, though some rules must be followed for that to work.

App.jsx is the entry point of our file.

.render is like method chain

:root {
  font-family: system-ui, Avenir, Helvetica, Arial, sans-serif;
  line-height: 1.5;
  font-weight: 400;

  color-scheme: light dark;
  color: rgba(255, 255, 255, 0.87);
  background-color: #242424;

  font-synthesis: none;
  text-rendering: optimizeLegibility;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

a {
  font-weight: 500;
  color: #646cff;
  text-decoration: inherit;
}
a:hover {
  color: #535bf2;
}

body {
  margin: 0;
  display: flex;
  place-items: center;
  min-width: 320px;
  min-height: 100vh;
}

h1 {
  font-size: 3.2em;
  line-height: 1.1;
}

button {
  border-radius: 8px;
  border: 1px solid transparent;
  padding: 0.6em 1.2em;
  font-size: 1em;
  font-weight: 500;
  font-family: inherit;
  background-color: #1a1a1a;
  cursor: pointer;
  transition: border-color 0.25s;
}
button:hover {
  border-color: #646cff;
}
button:focus,
button:focus-visible {
  outline: 4px auto -webkit-focus-ring-color;
}

@media (prefers-color-scheme: light) {
  :root {
    color: #213547;
    background-color: #ffffff;
  }
  a:hover {
    color: #747bff;
  }
  button {
    background-color: #f9f9f9;
  }
}


class component is obsolete, we use fxnal component

use svg, it can adjust the width and heigh without tampering with the quality.
Svg takes lesser space.

svg2jsx.com.

the equivalent of class is className